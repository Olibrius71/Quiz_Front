import React from "react";
import {Container, Typography} from "../atoms";

const Clock = () => {
  return (
    <Container.Base>
      <Typography.Title text="Clock" />
    </Container.Base>
  );
};

export default Clock;
